# Validation Dataset

This folder contains validation images used during Phase-1 model training.

The validation dataset is used to monitor model performance, tune parameters, and prevent overfitting.

Class sub-folders match exactly with the training and test dataset classes.
